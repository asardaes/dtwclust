* compare_clusterings
    + optimize preprocessing
    + flatten config
    + add results data frame
    + names
* tsclust
    + check if `pam.precompute = FALSE` can be optimized
* cvi
    + fuzzy cvis?
